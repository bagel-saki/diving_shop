<div class="c-pagination">
  <div class="pagination">
  <nav class="navigation">
    <div class="nav-links u-desktop-flex">
      <a class="prev page-numbers" href="#"><span></span></a>
      <% for (let i = 1; i <= 6; i++) { %>
        <% if (i === 1) { %>
          <a class="page-numbers current js-pagination"><%= i %></a>
        <% } else { %>
          <a class="page-numbers js-pagination" href="#"><%= i %></a>
        <% } %>
      <% } %>
      <a class="next page-numbers" href="#"><span></span></a>
    </div>
    <div class="nav-links u-mobile-flex">
      <a class="prev page-numbers" href="#"><span></span></a>
      <% for (let i = 1; i <= 4; i++) { %>
        <% if (i === 1) { %>
          <span class="page-numbers current"><%= i %></span>
        <% } else { %>
          <a class="page-numbers" href="#"><%= i %></a>
        <% } %>
      <% } %>
      <a class="next page-numbers" href="#"><span></span></a>
    </div>
  </nav>
  </div>
</div>